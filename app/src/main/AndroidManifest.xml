<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wiktor.udemyrazdel9all">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".lessons.lesson63.ScoreActivityLesson63"></activity>
        <activity android:name=".lessons.lesson63.Lesson63" />
        <activity android:name=".lessons.Lesson62" />
        <activity android:name=".lessons.Lesson61" />
        <activity android:name=".lessons.Lesson60" />
        <activity android:name=".lessons.Lesson58" />
        <activity android:name=".lessons.Lesson57" />
        <activity android:name=".lessons.Lesson56" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>